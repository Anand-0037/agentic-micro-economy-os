# Worker env template — copy values from repo root `.env.example`

MANTLE_RPC_URL=https://rpc.sepolia.mantle.xyz
MANTLE_CHAIN_ID=5003
TREASURY_EOA=0x47933b98Be8Feedf63dE822B3ce7408D027B4108
AGENT_IDENTITY_ADDRESS=0xEc14f781DB5f5f350F26Bc10Fb8f654e1D91daCc
AGENT_TOKEN_ID=0
EXECUTION_ADAPTER=fusionx_v2
WORKER_MODE=live_limited
LIVE_ENABLED=true

# === OPERATOR GATES (real production prep - do these first) ===
# 1. Fresh EOA in air-gapped terminal (not Cursor). Fund ≥0.5 MNT.
# 2. Rotate MANTLESCAN_API_KEY at mantlescan.xyz/myapikey.
# 3. Render: Starter plan + optional 1GB disk at /data → MEMORY_DB_PATH=/data/ameo.db + /health check.
# 4. Deploy AgentIdentity V2 (see contracts script) → set AGENT_IDENTITY_ADDRESS_V2.
# 5. One real cycle: observe → real volatility/policy → FusionX settle → DecisionLogged + 0G → /v1/verify clean.
