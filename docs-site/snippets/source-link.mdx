export const SourceLink = ({ file }) => (
  <Card
    title="View source on GitHub"
    icon="github"
    href={`https://github.com/AnandVashisht15/turing-test/blob/main/docs/${file}`}
  >
    The same Markdown that renders here is the source of truth in the repo.
  </Card>
);
